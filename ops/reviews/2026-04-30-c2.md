# Cycle #2 REVIEW — 2026-04-30 (두 번째 사이클)

11+1 proposals 합성. score = 트래픽 효과 (1~5) × 구현 용이도 (1~5) ÷ 회귀 위험 (1~3). 외부 의존 격리 0건.

## 1. 합성 요약

Cycle #1 자산(Org+WebSite·BlufBox·llms-full)을 횡전개하면서 **고립 페이지 31건 해소**·**모바일 Perf 96→100 시도**·**119 subsidy 페이지의 GovService 신호 강화**가 핵심 축.

## 2. P0 (본 사이클 EXECUTE — 8건)

| # | 항목 | 효과 | 용이 | 위험 | score | 출처 |
|---|---|---:|---:|---:|---:|---|
| **P0-1** | Footer 카테고리 7개 링크 (고립 페이지 7건 즉시 해소) | 4 | 5 | 1 | **20** | explore-orphans |
| **P0-2** | GovernmentService.isBasedOn = applyUrl (119 subsidy 일괄, GEO 핵심 신호) | 4 | 5 | 1 | **20** | general-tech-seo |
| **P0-3** | 토픽 페이지 `/issues/topics/[term]/` 하단 "다른 토픽 보기" nav 8개 | 4 | 5 | 1 | **20** | explore-trending-hub |
| **P0-4** | glossary [id] 30 페이지에 BlufBox (term + 정의 + 적용 지원금 N건) | 4 | 4 | 1 | **16** | general-geo |
| **P0-5** | vitals 동적 import → requestIdleCallback 디퍼 + 무한 keyframe contain | 3 | 5 | 1 | **15** | general-cwv |
| **P0-6** | sitemap filter `/issues/main/` + JSON-LD @id 중복 가드 + buildCollectionPage 빈 가드 | 3 | 4 | 1 | **12** | general-tech-seo + review-regression |
| **P0-7** | subsidy [id] FAQ + rejectionReasons H2 + FAQPage schema (topic 매칭) | 5 | 4 | 2 | **10** | general-content-seo |
| **P0-8** | keyword-coverage CATEGORIES `label` → `name` hotfix (audit 정확도 회복) | 1 | 5 | 1 | **5** | audit-tools |

**P0 8건 합계** — Cycle #1과 동일 부하. 모두 외부 의존 0.

## 3. P1 (다음 사이클 후보)

| 항목 | score | 사유 |
|---|---|---|
| topics/[term] BlufBox 자동 통계 | 12 | 트래픽 효과 있으나 P0-3 완료 후 적용이 자연스러움 |
| llms-full subsidies 본문 확장 (eligibility/benefits/documents) | 12 | 169KB → 200KB 한도 — 신중 검토 필요 |
| entity-graph 정적 인덱서 (옵션 C) | 10 (큰 작업) | Cycle #3에서 단독 commit으로 충분히 시간 들여 |
| categories 페이지 cross-ref hub 그리드 | 8 | 데이터 부족 — Cycle #3 |
| Claude API prompt caching | 5 | 트래픽 직결 X (토큰 절감만) |
| check-apply-urls 호스트 화이트리스트 | 5 | 보안 — 트래픽 X |
| generate-issue-posts 입력 30KB 캡 | 5 | 비용 가드 |
| HotkeyNav focus trap | 4 | A11y 100 유지 회귀 방지 |
| 홈 하단 "트렌딩 토픽" 섹션 | 4 | 홈 인라인 CSS 변동 — 신중 |
| sitemap matrix 테스트 (단위 테스트) | 6 | 인프라 — 가치는 회귀 방지 한정 |

## 4. P2 (백로그)

- CSP report-only 자가관측
- heading hierarchy lint
- prefers-contrast: more 토큰
- skip-link Playwright 회귀 테스트
- comparison schema (`<table>` + ItemList)
- structured outputs (Claude messages.json_schema)
- batch API
- internal-link-audit PageRank 추가
- build timing audit

## 5. 외부 의존 격리 → backlog-external.md

본 사이클에서 격리된 항목 0건. 모든 11+1 proposals가 제약 준수.

## 6. 회귀 위험 / 주의

- **P0-1 (Footer)**: Footer.astro 시각 회귀 — 컬럼 수 증가 시 모바일 폭 좁아짐 검토. flex-wrap 확인.
- **P0-2 (isBasedOn)**: 기존 GovService schema에 추가 — Google rich result 검증 (Schema.org GovernmentService 표준 필드).
- **P0-3 (토픽 nav)**: history.json totalCount ≥ 3 임계 미달 토픽은 링크 없는 회색 표시 (사용자 혼란 방지).
- **P0-4 (glossary BlufBox)**: 30 페이지 일괄 — DefinedTerm schema와 컴포넌트 충돌 없음 검증.
- **P0-5 (vitals defer)**: requestIdleCallback Safari 지원 — fallback `setTimeout(_, 2500)` 필수.
- **P0-6 (sitemap filter)**: filter 함수 추가 시 다른 라우트 의도치 않게 제외되는지 검토.
- **P0-7 (FAQ schema)**: matchedTopic 0 가드 필수. 중복 FAQPage schema 페이지당 1개만.
- **P0-8 (categories)**: keyword-coverage 결과 변화 — 다음 OBSERVE에서 categories 평균 score 측정 가능.

## 7. 다음 단계 → EXECUTE

P0 8건 순서:
1. **P0-8** (categories hotfix — 가장 작음, 측정 도구 우선)
2. **P0-6** (회귀 차단 — sitemap·schema 가드)
3. **P0-1** (Footer — 고립 페이지 즉시 해소, 31 → 24)
4. **P0-2** (isBasedOn — schema 헬퍼 1줄)
5. **P0-3** (토픽 hub nav — 트래픽 직결)
6. **P0-5** (CWV defer + contain — 모바일 Perf 시도)
7. **P0-4** (glossary BlufBox — 30 페이지 적용)
8. **P0-7** (FAQ + rejectionReasons — 가장 복잡, 마지막)

각 항목 별도 커밋, 완료 후 `lint + lint:content + check + build` 일괄. 푸시는 사용자 명시 시에만.
