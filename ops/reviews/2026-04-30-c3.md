# Cycle #3 REVIEW — 2026-04-30 (세 번째 사이클)

11 proposals 점수화. score = 효과 (1~5) × 용이 (1~5) ÷ 위험 (1~3). 외부 의존 격리 0건.

## 1. 합성 요약

Cycle #2 OBSERVE에서 발견된 **회귀 5건 (duplicate_@id) + audit 정확도 한계**를 우선 차단하면서, **콘텐츠 SEO 풍부화 (FAQ 80→100% 커버) + GEO 청크 (subsidy BLUF + topics BlufBox) + 보안 P1 본격화**가 핵심 축.

## 2. P0 (본 사이클 EXECUTE — 8건)

| # | 항목 | 효과 | 용이 | 위험 | score | 출처 |
|---|---|---:|---:|---:|---:|---|
| **P0-1** | 6 hub에 `collectionSchema && ...` 가드 + null literal 검출 (회귀 차단, 잠복 누수 해소) | 4 | 5 | 1 | **20** | review-regression |
| **P0-2** | duplicate_@id 5건 해소 (BaseLayout Prop 추가 + about/index inline orgSchema 제거) + internal-link-audit URL decode | 4 | 4 | 1 | **16** | explore-regression |
| **P0-3** | topics.json 카테고리 공통 FAQ 보강 (5×3=15건) + subsidy [id] FAQ fallback (`commonEligibility`) | 5 | 4 | 1 | **20** | general-content-seo |
| **P0-4** | subsidies/[id] BLUF 박스 (대상·금액·마감·신청처) — 119건 일괄 | 5 | 4 | 1 | **20** | general-geo |
| **P0-5** | NewsArticle.publisher → `#organization` @id 참조 | 3 | 5 | 1 | **15** | general-tech-seo |
| **P0-6** | topics/[term] BlufBox 자동 통계 (totalCount·daysActive·matchedSubsidies·peakDay) | 3 | 4 | 1 | **12** | general-geo |
| **P0-7** | check-apply-urls 호스트 화이트리스트 (.go.kr/.gov.kr/.or.kr) + generate-issue-posts 입력 30KB 캡 | 2 | 5 | 1 | **10** | security-review |
| **P0-8** | Claude API system prompt cache_control: ephemeral (35% 토큰 절감) | 1 | 5 | 1 | **5** | claude-api |

**P0 8건 — 모두 외부 의존 0.**

## 3. P1 (다음 사이클 후보)

| 항목 | score | 사유 |
|---|---|---|
| entity-graph 정적 인덱서 + reader (Cycle #1·#2·#3 보류) | 8 | 단독 사이클로 진행 권장 — 다른 P0와 충돌 시간 분리 |
| IncomeChecker React → vanilla + @astrojs/react 제거 | 8 (위험 2) | 홈 -186KB 라이브러리 큰 효과지만 폼 UI 재현 검증 필요 — 단독 PR로 충분히 시간 |
| `<time datetime>` 마크업 + GovernmentService.dateModified | 12 | P0-3·4와 묶을지 별도일지 검토. 본 사이클은 P0-3에 포함시킬 가능성 |
| HotkeyNav focus trap (WCAG) | 10 | 트래픽 효과는 작음 — 회귀 방지 측면 |
| inline-glossary 자동 anchor | 6 | entity-graph 완성 후 |
| /guide HowTo schema | 9 | 단독 추가 가능하나 P0 8건이 우선 |
| /quick WebApplication schema | 8 | 위와 동일 |
| RSS `<dc:creator>` + `<atom:updated>` | 6 | 작은 효과 |
| buildBreadcrumb @id 추가 | 6 | 7~10곳 마이그레이션 점진적 |
| sitemap allow-list 전환 | 4 | 미래 회귀 차단 — P2 |
| heading hierarchy 빌드 게이트 + skip-link Playwright | 6 | 인프라 작업 |
| fact-check 누적 카운터 | 4 | OBSERVE 통합 |
| /quick/ + /guide/ BlufBox | 6 | 단순 적용 가능 |

## 4. P2 (백로그)

- CSP nonce 마이그레이션 (대규모)
- isBasedOn → primarySources 콘텐츠 백필 (의미 정정)
- /q/ 쿼리형 라우트 (옵션 C — 추측 위험)
- /decide/[topic-id]/ 라우트 분리 (옵션 B — 콘텐츠 중복 우려)
- subsidy 카테고리 자동 H2 (페르소나 기반 sub-headline)
- issues 트렌딩 검색 hint
- `prefers-contrast: more` 토큰

## 5. 외부 의존 격리

본 사이클 PLAN 11건 모두 외부 의존 0 — 격리 0건.

## 6. 회귀 위험 / 주의

- **P0-1 (collectionSchema 가드)**: 6 hub 페이지 일괄 변경 — 데이터 0건 케이스 없으니 즉각 효과 없으나 잠복 누수 해소. 회귀 위험 없음
- **P0-2 (orgSchema 제거)**: about/index의 inline schema 제거 시 BaseLayout 출력으로 대체. duplicate_@id 5건 → 0 회복 검증 필요
- **P0-3 (topics 보강)**: topics.json 신규 5종 추가 — content schema 검증 (zod) 통과 필수
- **P0-4 (subsidy BLUF)**: 119 페이지 적용 — BlufBox 컴포넌트 재사용. 데이터 추출 (s.amount/s.deadline/s.applyUrl/audienceTypes) 정확성
- **P0-5 (NewsArticle @id)**: 2건만 영향 (현재 issue post)
- **P0-6 (topics/[term] BlufBox)**: 활성 토픽 2건 — 작은 영향
- **P0-7 (security)**: check-apply-urls는 외부 API 호출 시 검증 강화 — 정상 정부 URL은 모두 통과해야 함
- **P0-8 (Claude API caching)**: 1회차 호출은 캐시 미스(full cost), 2회차+부터 절감 — 첫 사이클 동안은 효과 모니터링 필요

## 7. 다음 단계 → EXECUTE

P0 8건 순서:
1. **P0-1** (회귀 차단 — collectionSchema 가드, 가장 먼저)
2. **P0-2** (duplicate_@id 해소 + audit URL decode)
3. **P0-5** (NewsArticle.publisher @id — 작은 단일 변경)
4. **P0-7** (보안 — check-apply-urls + 입력 캡)
5. **P0-8** (Claude API caching — 비용 가드)
6. **P0-3** (topics 카테고리 공통 FAQ 보강 — 콘텐츠 변경)
7. **P0-4** (subsidies BLUF 박스 — 119 페이지 일괄)
8. **P0-6** (topics/[term] BlufBox)

각 항목 별도 커밋, 검증 게이트 일괄 후 OPERATE → OBSERVE → main 머지 (푸시는 사용자 명시 시).
