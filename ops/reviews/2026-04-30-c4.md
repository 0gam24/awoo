# Cycle #4 REVIEW — 2026-04-30 (네 번째 사이클)

11 proposals 점수화. 외부 의존 격리 0건.

## 1. 합성 요약

Cycle #3 회귀 점검 결과 4건 잠복(category enum·BLUF null·hostname·cache log) + 미적용 hub 2건 BLUF + Cycle #1·#2·#3 누적 P1(time datetime, focus trap, RSS dc:creator, factCheckFails) 일괄 흡수. **entity-graph + IncomeChecker vanilla**는 단독 사이클(#5)에서 본격 진행하도록 P1로 분리.

## 2. P0 (본 사이클 EXECUTE — 8건)

| # | 항목 | 효과 | 용이 | 위험 | score | 출처 |
|---|---|---:|---:|---:|---:|---|
| **P0-1** | Cycle #3 회귀 4건 묶음 (subsidy category enum + BLUF null 가드 + check-apply-urls hostname + Claude cache log) | 4 | 4 | 1 | **16** | review-regression |
| **P0-2** | `<time datetime>` + GovService/NewsArticle.dateModified + subsidy 카테고리 자동 H2 | 5 | 4 | 1 | **20** | general-content-seo |
| **P0-3** | /quick + /guide BlufBox + llms-full subsidies coreFacts 블록 | 4 | 4 | 1 | **16** | general-geo |
| **P0-4** | /quick compactSubsidies `summary` 필드 제거 (-16KB) | 3 | 5 | 1 | **15** | general-cwv |
| **P0-5** | buildBreadcrumb `@id` opt-in + RSS dc:creator + atom:updated | 3 | 5 | 1 | **15** | general-tech-seo |
| **P0-6** | focusTrap.ts + HotkeyNav 적용 + audit-headings.mjs postbuild 게이트 | 2 | 4 | 1 | **8** | general-a11y-bp |
| **P0-7** | _history.json factCheckFails + cycle-runner OBSERVE 7일 합계 알림 | 2 | 4 | 1 | **8** | security-review |
| **P0-8** | `_cache-{date}.json` 영속 로깅 (Claude API 운영 모니터링) | 1 | 5 | 1 | **5** | claude-api |

**P0 8건 — 모두 외부 의존 0.**

## 3. P1 (다음 사이클 후보)

| 항목 | score | 사유 |
|---|---|---|
| **entity-graph 정적 인덱서 + inline-glossary** | 10 | Cycle #5 단독 진행 권장 (build-entity-graph + reader + schema-validate + inline anchor 묶음, 1.5h) |
| **IncomeChecker React → vanilla + @astrojs/react 제거** | 10 | Cycle #5 단독 PR 권장 (-186KB 라이브러리, 위험 2 — UI 재현 검증 필요) |
| /guide HowTo schema + /quick WebApplication schema | 9 | P0 6번 묶음 후 추가 검토 — schema 중복 검증 필요 |
| Claude API 정적 컨텍스트 추가 cache block | 6 | _cache-{date}.json 1주 측정 후 |
| Claude Structured Outputs (json_schema) | 5 | 콘텐츠 생성 안정화 후 |
| /subsidies/ 카드 data-attr 통합 (-21KB) | 7 | size guard 마진 충분, P1 |
| skip-link 회귀 audit (vanilla 검증) | 4 | 인프라 |
| inline-glossary 자동 anchor | entity-graph 후속 | |

## 4. P2 (백로그)

- inlineStylesheets 'always' → 'auto' (Cycle #1·#2·#3·#4 4번째 보류)
- CSP nonce 마이그레이션
- Haiku 4.5 + thinking 비교 실험
- isBasedOn → primarySources 콘텐츠 백필
- /q/ 쿼리형 라우트
- /decide/[topic-id]/ 라우트 분리
- i18n 영문 hub `/en/` (신뢰성 리스크)
- prefers-contrast: more 토큰
- Lighthouse CI CrUX field data 모니터링

## 5. 외부 의존 격리

본 사이클 PLAN 11건 모두 외부 의존 0 — 격리 0건.

## 6. 회귀 위험 / 주의

- **P0-1 (회귀 차단)**: subsidy category enum은 zod schema와 site-data CATEGORIES 동기화. BLUF null 필터는 컴포넌트 단에서 자동 (.filter)
- **P0-2 (time datetime)**: lastVerifiedISO 헬퍼는 이미 존재. Schema dateModified는 옵셔널이므로 회귀 X
- **P0-3 (BlufBox 추가)**: /quick·/guide hero 영역 — Footer 기존 마크업과 충돌 X 검증 필요
- **P0-4 (summary 제거)**: /quick 매칭 로직이 summary 사용 안함 확인 (검색 hint 노출에는 영향 X)
- **P0-5 (RSS)**: xmlns:dc 선언 위치, atom:updated ISO 형식 정확성
- **P0-6 (focusTrap)**: HotkeyNav 외 다른 모달이 추가될 때 재사용성 — 처음부터 범용 팩토리로
- **P0-7 (factCheckFails)**: _history.json 스키마 변경은 atomic write 필수
- **P0-8 (cache 로깅)**: 빌드 산출물 영향 X (스크립트 실행 시점 로그)

## 7. 다음 단계 → EXECUTE

P0 8건 순서:
1. **P0-1** (회귀 차단 4건 묶음 — 가장 먼저)
2. **P0-5** (Tech SEO — buildBreadcrumb + RSS, 기반 인프라)
3. **P0-2** (Content SEO — time datetime + 카테고리 H2)
4. **P0-3** (GEO — BlufBox 2건 + llms-full coreFacts)
5. **P0-4** (CWV — /quick summary 제거)
6. **P0-6** (A11y — focusTrap + heading lint)
7. **P0-7** (보안 — factCheckFails)
8. **P0-8** (Claude API caching log)

각 항목 별도 커밋, 검증 게이트 일괄 후 OPERATE → OBSERVE → main 머지.
