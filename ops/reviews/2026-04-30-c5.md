# Cycle #5 REVIEW — 2026-04-30 (다섯 번째 사이클)

11 proposals 점수화. score = 효과 (1~5) × 용이 (1~5) ÷ 위험 (1~3). 외부 의존 격리 0건.

## 1. 합성 요약

**4사이클 보류 청산 사이클**. entity-graph + IncomeChecker vanilla + inline-glossary는 Plan 옵션 A로 묶음. 추가로 schema 채널 확장(HowTo/WebApplication), GEO 인덱스 보강(llms.txt), CWV 큰 절감(/subsidies -18KB), 회귀 차단·운영 안정성을 균형있게.

## 2. P0 (본 사이클 EXECUTE — 8건)

| # | 항목 | 효과 | 용이 | 위험 | score | 출처 |
|---|---|---:|---:|---:|---:|---|
| **P0-1** | build-entity-graph.mjs + entity-graph.json + schema-validate dangling @id 가드 | 4 | 4 | 1 | **16** | explore-entity-graph |
| **P0-2** | inline-glossary 자동 anchor (subsidy/topic/situation 본문, 빌드타임 string replace) | 4 | 3 | 2 | **6** | general-content-seo |
| **P0-3** | /guide HowTo schema + /quick WebApplication schema | 5 | 4 | 1 | **20** | general-tech-seo |
| **P0-4** | llms.txt 카테고리·상황·용어집 인덱스 + /about AboutPage + /editorial-policy Article | 4 | 4 | 1 | **16** | general-geo |
| **P0-5** | /subsidies/ 카드 data-attr JSON 통합 (-18KB) + lighthouserc INP error 승격 | 3 | 4 | 2 | **6** | general-cwv |
| **P0-6** | audit-skip-link.mjs + prefers-contrast: more 토큰 + heading audit strict 전환 | 2 | 4 | 1 | **8** | general-a11y-bp |
| **P0-7** | Claude API 재시도 (429/503 backoff) + _fail 7일 누적 + .gitignore 보강 | 2 | 4 | 1 | **8** | security-review |
| **P0-8** | feed-issues.xml RSS lint audit + Claude 정적 컨텍스트 cache 블록 | 2 | 5 | 1 | **10** | review-regression + claude-api |

**P0 8건 — 모두 외부 의존 0.**

**IncomeChecker vanilla 전환은 P1 단독 PR 권장** — UI 재현 검증 필요, 본 사이클은 entity-graph + inline-glossary 묶음에 집중.

## 3. P1 (다음 사이클)

- **IncomeChecker React → vanilla + @astrojs/react 제거** (단독 PR)
- /subsidies/ data-attr 추가 절감 후속
- subsidies/[id] mainEntityOfPage
- glossary DefinedTermSet @id 강화
- llms-full categories 섹션
- issues NewsArticle audience
- subsidy 본문 footer + glossary "다음 용어"
- _history.json atomic write
- /api/vitals rate limit
- Claude Structured Outputs (1주 _cache 데이터 후)

## 4. P2 (백로그)

- inlineStylesheets 'auto' (5번째 보류 → Cycle #6+)
- CSP nonce 마이그레이션
- Haiku 4.5 + thinking 실험
- /q/ 쿼리형 라우트
- /decide/[topic-id]
- i18n 영문 hub

## 5. 외부 의존 격리

본 사이클 PLAN 11건 모두 외부 의존 0 — 격리 0건.

## 6. 회귀 위험 / 주의

- **P0-1 (entity-graph)**: prebuild hook 추가 시 build 명령 체이닝 + CI 실행 시간 검증
- **P0-2 (inline-glossary)**: 빌드타임 string replace — 정규식 안전성·HTML 파싱 X (단순 치환), 첫 등장 1회 cap 정확성
- **P0-3 (HowTo/WebApp)**: Schema.org 표준 필드 검증 — Google rich-result Test 통과 필수
- **P0-4 (llms.txt 보강)**: 30KB → 약 35~40KB로 증가, AI 크롤러 fetch 부담 미미
- **P0-5 (/subsidies data-attr 통합)**: 정렬 스크립트 동시 수정, 기존 sort UI 동작 확인 필수
- **P0-6 (a11y strict)**: heading audit warn → error 전환 시 빌드 즉시 fail 가능 — 현재 위반 0 데이터로 안전
- **P0-7 (재시도 backoff)**: setTimeout 사용, max 3회 + log 누적
- **P0-8 (RSS lint)**: feed-issues.xml + feed.xml 둘 다 검증

## 7. 다음 단계 → EXECUTE

P0 8건 순서:
1. **P0-8** (RSS lint + Claude cache — 회귀 가드·작은 변경)
2. **P0-1** (entity-graph — 다른 P0가 활용할 자산)
3. **P0-3** (/guide HowTo + /quick WebApplication)
4. **P0-4** (llms.txt + AboutPage + Article)
5. **P0-2** (inline-glossary anchor — entity-graph 후속)
6. **P0-6** (audit-skip-link + prefers-contrast + heading strict)
7. **P0-5** (/subsidies data-attr 통합)
8. **P0-7** (Claude 재시도 + _fail 7일 + .gitignore)

각 항목 별도 커밋, 검증 게이트 일괄 후 OPERATE → OBSERVE → main 머지.
