# NAVER API 한도와 실사용량

기준 2026-09-09 · Ncloud 콘솔 > API 관리 (Application `awoo`) 실측.

## 1. 한도

| 그룹 | 포함 API | 당일 | 당월 |
|---|---|---|---|
| **NAVER 검색** | 이미지 · 지식iN · 성인검색어 판별 · 블로그 · 오타변환 · **뉴스** · **웹문서** · 카페 | 25,000 | 775,000 |
| **DataLab** | 검색어트렌드 | 제한 없음 | 50,000 |

검색 8종은 **한 덩어리로 한도를 공유한다.** 블로그를 많이 부르면 지식iN 몫이 줄어든다.

## 2. 지금 쓰는 양 (2026-09-09 조사 폭 확대 후)

| 실행 | 주기 | 1회 호출 | 일 | 월 |
|---|---|---|---|---|
| `keyword-radar.mjs` | 하루 4회 | 검색 ~215 + 데이터랩 ~38 (2026-09-10 시드 7개·born 판정 추가 후 회차당 ~253) | 검색 ~860 · 랩 ~152 | 검색 ~25,800 · 랩 ~4,560 |
| `sync-issues.mjs` | 하루 2회 | 검색 3 | 검색 6 | 검색 180 |
| **합계** | | | **검색 ~866 / 25,000 (3.5%)** | **검색 ~26,000 / 775,000 (3.4%) · 랩 ~4,560 / 50,000 (9.1%)** |
| `keyword-pipeline.mjs` | 하루 1회 | 정찰 ≤25쿼리 × 2(웹문서·뉴스) = **검색 ≤50** | 검색 ≤50 | 검색 ≤1,500 |
| `naver-rank-check.mjs` | 하루 1회 | 측정 ≤25쿼리 × 2(웹문서·뉴스) = **검색 ≤50** | 검색 ≤50 | 검색 ≤1,500 |

**2026-09-15 전환:** 순위 측정·정찰이 통합검색 HTML 수집에서 **공식 웹문서 검색 API**로 바뀌었다(robots.txt `Disallow: /`). 쿼리당 웹문서 1 + 뉴스 1 = 2호출이고, 둘 다 검색 그룹 한도를 쓴다. 하루 최대 100회 — 한도 25,000의 0.4%다.

레이더 내역(2026-09-10): 지식iN 시드 35(4차·추석·설 민생지원금·조례·가결·신청·지급일 추가) + 시장조사 60개 × (블로그·카페·뉴스) 180 + 데이터랩 born 판정 묶음. 정확한 회차 수치는 `node scripts/keyword-radar.mjs --dry-run` 마지막 줄의 사용량 보고를 보라.

**여유는 여전히 검색 월 75만회, 데이터랩 월 4.7만회다.**

### 확대로 실제로 달라진 것

| | 확대 전 | 확대 후 |
|---|---|---|
| 지식iN 시드 | 10개 | **28개** |
| 시드당 수집 | 30건 | **100건** (API 상한, 호출 수 동일) |
| 수집 질문 | 280건 | **2,661건** |
| 추출 키워드 | 20개 | **118개** |
| 데이터랩 조사 | 20개 | 40개 |
| 시장조사(공급량) | 15개 | 60개 |
| 1회 실행 시간 | 25초 | **4분 31초** |

**병목은 API 한도가 아니라 수집량이었다.** 확대 전에는 후보 term이 20개뿐이라
시장조사 대상을 60개로 늘려도 채울 것이 없었다. `display`를 30→100으로 올린 것이
가장 크게 작용했다 — **호출 수는 그대로인데 수집량이 3.3배**가 된다.

## 3. 집계 방식

`scripts/lib/api-quota.mjs`가 호출 지점마다 `countCall(group, name)`으로 센다.
실행 끝에 `formatUsage()`가 콘솔에 찍고, 레이더는 `keyword-radar.json`의 `apiUsage`에도 남긴다.

- 실제 누적이 아니라 **"이 스케줄대로 돌면 얼마를 쓰는가"의 산출값**이다.
  실누적은 Ncloud 콘솔에서 본다.
- cron을 바꾸면 각 스크립트의 `RUNS_PER_DAY` 상수도 같이 바꿔야 추정이 맞는다.
- 한 실행이 500회를 넘으면 호출 루프 버그로 보고 즉시 예외를 던진다(`RUN_CEILING`).

## 4. 남은 여유로 더 할 수 있는 것 (미실행)

지금도 검색 3.4% / 데이터랩 6.2%다. 아래는 아직 안 한 것들이다.

| 안 | 변경 | 변경 후 |
|---|---|---|
| 시드 재확대 | 28 → 60개 | 질문 약 5,700건, 검색 일 4%대 |
| 순위 측정 확대 | 25건 → 60건 | API 호출 120회(한도의 0.5%). 수집 예의 때문에 25로 묶을 이유가 없어졌다 — 상한은 운영자 결정 §7-7 사항 |

**웹문서 API(`/search/v1/webkr`)는 2026-09-15부터 순위 측정의 유일한 경로다.** API 결과와 통합검색 화면 노출은
같지 않다 — 실측 8건 비교에서 화면 3위가 API 15위, 화면 미노출이 API 1위였다. 그래서 API 순위는
"웹문서 색인 안의 자리"로만 읽고, 화면에서의 실제 노출·위치는 ① 운영자 눈 확인(`eyeOffset`)과
② 네이버 서치어드바이저 실측(노출수·평균순위)으로 본다(`docs/ops/NAVER-SEARCH-ADVISOR.md`).

## 5. 키

- HUB(Ncloud): `NCP_API_KEY_ID` / `NCP_API_KEY` → 헤더 `X-NCP-APIGW-API-KEY-ID` / `X-NCP-APIGW-API-KEY`
- 레거시 오픈API: `NAVER_CLIENT_ID` / `NAVER_CLIENT_SECRET`
- **둘 다 GitHub Secrets에만 둔다.** NCP 키가 있으면 자동으로 HUB를 쓰고, 없으면 레거시로 떨어진다.
  전환·롤백 모두 시크릿만 넣고 빼면 된다. 코드 변경 불필요.
